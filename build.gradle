plugins {
    id "java-gradle-plugin"
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}

gradlePlugin {
    plugins {
        cloudCommanderModelsPlugin {
            id = "com.cloudcommander.models"
            implementationClass = "com.cloudcommander.gradle.plugins.models.ModelsPlugin"
        }
    }
}